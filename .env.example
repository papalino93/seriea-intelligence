# Supabase — Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# football-data.org — free tier, 10 richieste/minuto, stagione corrente inclusa
FOOTBALL_DATA_API_KEY=

# Stringa a caso (es. openssl rand -hex 32). Serve solo per un eventuale
# trigger esterno futuro di /api/sync — il pulsante admin non la usa.
SYNC_SECRET=
